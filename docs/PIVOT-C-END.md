# 转向：从「投顾训练器」到「C 端异动干预」

> 写于 2026-08-22（第十个会话末尾）。**这是一份执行清单，不是已完成的事。**
> 四项任务全部未开工，下一个会话按这份文件做。
>
> 先读 [POSITIONING.md](POSITIONING.md)（现行定位，本次要被改写的那一页），
> 再读 [HANDOFF.md](HANDOFF.md) 开头三条（网关状态决定任务二能不能开工）。

## 为什么要转

这轮评审拿东方财富的公司背景重新量了一遍定位，结论是一条**选题错配**：

**东方财富是互联网券商，它的核心竞争力恰恰是不靠投顾人海。**
传统券商（中信、国君、银河）有庞大的营业部投顾队伍，一套投顾陪练在那里
有明确的业务 owner 和培训预算；东财的组织结构里这个角色的规模和中心度都低得多。

后果不是"市场窄"，是**内部找不到人接**：没有业务线 owner、没有预算、
没有推广渠道，评委席上大概率没有一个人的 KPI 与"投顾话术水平"直接相关。
拿到奖也只能停在 demo。

**但同一套资产换个对象，匹配度从最低变成最高：**

| 产品里已有的 | 东财真实拥有的 |
|---|---|
| 「资产异动预警：客户清仓 / 大额转出」 | 东财证券、天天基金**真实看得到这个信号**——产品里那条预警本来就是照它写的 |
| 老陈 / 周淑琴 / 老刘 / 月娥姐 / 顾之然 | 就是平台上的存量用户画像（含 29 岁的顾之然） |
| 荐股群、王老师、"内部消息" | 股吧是这类骗局在中文互联网的实际招募场之一 |
| 七把钥匙 + 闭集分类器 | 可**反向**用于识别诱导话术，不只用于训练 |

外加一条外部推力：《反电信网络诈骗法》给金融机构压了实打实的风险提示与
劝阻义务。**这不是"要不要做"，是"迟早要做"。**

> **判分引擎、五个场景、效力矩阵、482 个测试全部不动。**
> 这次要改写的是那一页定位，不是那一套代码。这一点必须先说清楚，
> 否则下一个人会以为要重做。

---

## 一、把定位改成「C 端异动干预」，写一页说明

**产出**：一页文档（建议直接改写 [POSITIONING.md](POSITIONING.md) 的「一句话」
与「成功标准」两节，旧版本留在 git 历史里），必须回答三个问题。

### 1. 接哪个信号

用户在东财证券 / 天天基金触发下列任一动作时进入：

- 清仓（持仓一次性归零）
- 大额转出（占账户资产比例超阈值）
- 与历史行为显著背离的赎回（如"五年只做定投的账户今天全部赎回"）

**这三条产品里已经有了**——`Scenario.facts` 那几行 `warn=True` 就是照它们写的
（chen「今日转出 ¥100,000 / 占 78%」、hang「五年零主动交易，今天一次清仓」）。
现在它们是布景，转向之后它们是**触发条件**。

### 2. 归哪条线

用户风险与合规条线（不是培训条线）。这一条决定了：
- owner 是谁、预算从哪出
- 评委问「谁接」时有答案
- 合规义务是推力而不是阻力

### 3. 怎么衡量效果

**不再用"玩家打了几局"这类参与度指标**，改用干预效果：

| 指标 | 口径 |
|---|---|
| **24h 内转出取消率** | 干预组 vs 对照组，触发后 24 小时内主动撤销转账/赎回的比例 |
| **24h 内撤单率** | 同上，针对已提交未完成的指令 |
| 完成率 | 三分钟对话走完的比例（低了说明太长） |
| 二次触发率 | 30 天内再次触发异动的比例 |

**必须能和对照组比**——没有对照组的干预率是无法解释的数字。

### 4. 形态本身

用户按下转账/赎回确认之前，App 里弹一次三分钟对话：
**让他扮演劝阻者，去劝一个和他处境一模一样的人。**

它为什么比风险提示弹窗强：**用户劝不动"老陈"的那一刻，会自己想到自己。**
这不是培训，是干预；不需要说服任何人来"练习"。

**产品定位一句话**：
> 不是投顾训练器，是东财在用户按下转账前的最后一次干预。

### 不做什么（沿用 POSITIONING 的写法）

- 不做成"考试"或"必须通过才能转账"——那会变成阻挠交易，合规上站不住，
  产品上也会被用户当障碍绕过
- 不在干预里给任何投资建议（现有的两条合规红线继续管着）
- 不因为转向就动判分闭集（七把钥匙、效力矩阵、五个场景一行不改）

---

## 二、跑一次妙想的分类对照评测，报两栏表

**为什么这件事性价比最高**：内部赛上「为什么不用妙想」几乎是必问题，
而这个项目**恰恰是全场最适合换模型的那一个**，却没把这一点讲出来。

> ADR-0001 把判分交给纯函数规则表，模型只做两件事：演绎 + **闭集分类**。
> 分类是典型的垂直模型能干好的任务，而**判分正确性完全不受模型能力影响**。
> 换模型只影响演绎质感和分类准确率，不影响任何一个分值——
> 这是三个月前就买好的单，现在到了兑现的时候。

### 怎么做

1. 摸清妙想的接入方式（endpoint、模型 id、协议是 OpenAI 兼容还是别的）。
   现有的 `app/llm.py`（ChatCompletions）与 `app/anthropic_client.py`（Messages）
   已经是两套协议实现、同一个端口，**加第三套照着 `anthropic_client.py` 写**，
   网关与编排层不用改（§6.4 的分层就是为这个准备的）。
2. `tools/classify_eval.py` 跑 199 条标注集，**连跑两次**
   （§6.4：没有 temperature 了，同一份标注集两次连跑固有差 0.6~1.3 个百分点，
   小于 1.5 个百分点的变化不能当信号读）。
3. 报表已经现成：`accuracy_by_sid` 会按五个场景拆开。

### 产出：两栏表

| | claude-opus-5 | 妙想 | 门槛 |
|---|---|---|---|
| `hit_keys` 准确率（两次） | 90.5% / 89.9% | ? | ≥ 85% |
| `grounded` 全样本 | 94.2% / 93.7% | ? | ≥ 80% |
| `grounded` 有钥匙子集 | 96.6% / 97.4% | ? | ≥ 80% |
| `grounded` 复读攻略子集 | 100% / 100% | ? | ≥ 93% |
| 五个场景分栏 | 见 §9.3 | ? | — |
| 首句延迟 | — | ? | ≤ 3s |

**左栏的数字已经有了**（2026-08-22 实测，写在 TECH-DESIGN §9.3）。
只差右栏。

> **前置条件：网关。** 8-22 当天网关先后返回 `ip restriction!` 与
> `401 该令牌状态不可用`，后者持续 8 小时以上。开工前先探一次：
> ```
> .venv/bin/python -c "import asyncio;from app.llm import llm_client;print(asyncio.run(llm_client.probe()))"
> ```

---

## 三、服务端落对局数据 + 复盘砍到 5 块以内

### 3.1 落数据

**现状**：服务端**一条对局记录都没有**。`app/stats.py` 只有 5 个计数器
（games / turns / endings / hits / trust 分桶）。跑一万局也攒不下一条数据。

**为什么现在必须改**：形态 A（C 端干预）与形态 B（股吧治理）都要求落数据，
而这个赛道唯一可能的护城河就是数据飞轮——真实对话 → 专家标注 →
更准的分类器 + 更真的效力矩阵。**竞品抄走七把钥匙只需要一周，
抄不走三年的标注语料。**

**它会撞上 ADR-0003**（客户端持有签名状态、服务端不存任何东西）。
那条决策在比赛期是**完全正确**的取舍（部署简单、无状态、重启不丢局），
转向之后不再成立。**要新写一条 ADR 说明例外范围，不要偷偷改**——
ADR 的价值就在于反转时留下理由。

**两条硬要求**：
- **脱敏**：用户发言可能带姓名、金额、账号。落库前过一遍脱敏，
  且脱敏规则要有测试（可复用 `app/safety.py` 的思路）
- **明确告知**：8-22 已经在聊天页加了一行
  「对方是虚构客户，机构与人物均为虚构。本练习不构成投资建议；
  你输入的内容会发送至大模型用于生成回复。」
  落库之后这句话要**补上留存与用途**，且必须在用户开口之前出现

### 3.2 复盘砍到 5 块以内

**现状是 13 块**，打完 3 分钟要读十几屏：

结算卡 → 百分位 → 合规红线 → 手机揭晓 → 同一句话对照块 → 七把钥匙条 →
逐轮三段账 → 踩过的坑 → 别人打成什么样 → 本机训练记录 → 处置清单 →
分享卡 → 说明文

这违背了它自己引用的那条研究（PUBG 后置屏 N=12 用户研究：
**玩家不会为了看懂一个指标跑去别处找解释，看不懂就直接忽略**）。
8-22 还往里加了两块（对照块、处置清单），把问题加重了。

**C 端用户比投顾更没耐心**，转向之后这一条从"设计瑕疵"升级为"能不能用"。

建议保留的五块（可讨论，但总数是硬上限）：
1. 结算卡（他最后按没按下确认）
2. **同一句话，换个时候说**——全作品唯一竞品没有的判据，不能砍
3. 手机揭晓（他没说出口的那些）——这一块是"原来我也一样"的转折点
4. 处置清单（现实里还差这几步）——C 端干预的落点
5. 分享卡

**砍掉的不是删除，是收进折叠或移到"详细复盘"**。逐轮三段账、七把钥匙条、
本机记录这些对认真的人仍然有价值，只是不该挡在第一屏。

---

## 四、其余问题（上一轮评审提到、尚未处理）

按"转向之后还成不成立"标注。**⬆ 表示转向后变得更严重。**

| # | 问题 | 状态 |
|---|---|---|
| 1 | **前端 2462 行单文件、0 个测试**，而后端有 482 个。复盘承载全部教学价值，却没有任何自动化守护——`degraded` 那个 bug 潜伏这么久就是因为这个 | ⬆ 要接进 App 就要过前端工程规范 |
| 2 | **单一模型网关硬依赖，无第二供应商、无自动降级**。8-22 令牌停 8 小时，作品退化成 40 条兜底台词的抽卡机 | 不变，仍严重。另需一个**离线可演示模式**兜路演 |
| 3 | **`app/scenario.py` 1504 行，内容与代码焊死**。加一个骗局 = 一次研发排期，业务/运营改不了任何东西 | 不变 |
| 4 | **五个场景玩家不能选**（`pick_scenario` 均匀随机）。随机分配是游戏逻辑，定向练习才是训练逻辑 | 转向后可能反而**不需要**——干预场景应由用户自己的异动类型决定 |
| 5 | **没有重练闭环**：打完一局→复盘→结束。没有难度阶梯、没有针对弱项的定向练习 | 转向后降级（干预是一次性的） |
| 6 | **平衡参数是拍的**：`balance_sim` 的人群占比（novice 25% / expert 20% …）是内部假设，整套 12–22% 胜率带建立在它上面 | **有解了**：东财证券有真实用户行为分布，可校准 |
| 7 | **"能力可迁移"是循环论证**：最优解不同是因为我们把矩阵写成不同的 | **有解了**：股吧真实语料可做外部评测集，不必真人实验 |
| 8 | **形态 B 未启动**：把分类器反过来做股吧诱导话术识别。识别任务、真实语料、有 owner 有预算 | 新增，优先级仅次于本文四项 |
| 9 | 界面照搬微信像"公益 H5"而非企业级系统 | **降级**——形态 A 就长在 App 里，微信语法反而是对的 |
| 10 | 没做语音（同类产品 Yoodli / Second Nature 都做） | **降级**——C 端干预本来就是图文场景 |
| 11 | 业务规则从未经专家验证（审计 P0-7） | 不变。转向后应找**合规条线**而非培训条线把关 |

### 还欠着的跑批（与转向无关，网关一恢复就能做）

```bash
.venv/bin/python -m tools.act_eval --scenario ben  --concurrency 4 --dump baseline-ben-lines.jsonl
.venv/bin/python -m tools.act_eval --scenario hang --concurrency 4 --dump baseline-hang-lines.jsonl
.venv/bin/python -m tools.act_eval --scenario liu  --drift baseline-liu-lines.jsonl
.venv/bin/python -m tools.act_eval --scenario ben  --drift baseline-ben-lines.jsonl
.venv/bin/python -m tools.act_eval --scenario hang --drift baseline-hang-lines.jsonl
```

chen / zhou / liu 三份演绎基线与 chen / zhou 两份漂移已经跑完，见 §9.5。

---

## 执行顺序

**1 → 2 → 3.2 → 3.1 → 4**

理由：
- **任务一不需要写一行代码**，却是唯一能回答评委「谁接」的动作，先做
- **任务二只要一次跑批**，换一个必答题，性价比最高（但卡网关）
- **3.2（砍复盘）比 3.1（落数据）先做**：前者是纯前端、当天能完成、
  且转向之后是"能不能用"的前提；后者要动 ADR，慢
- 任务四按上表的 ⬆ 优先
