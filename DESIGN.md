# 视觉世界

> 写于 2026-09-04。**这一页管颜色、底、字、圆角，不管版式和文案。**
> 版式的理由散在 `static/style.css` 的注释里，文案的理由在
> [docs/POSITIONING.md](docs/POSITIONING.md)。

## 一条主张：三个世界，不是一套皮

这个作品从头到尾在模拟**三个真实存在的界面**，而每一次切换都是叙事的一步：

| 屏 | 模拟的是 | 视觉 |
|---|---|---|
| `#transfer` | 东方财富 App 的银证转账页 | **东财**：品牌橙 `#ea5404`、暖纸底 |
| `#assignment` `#opening` `#home` `#primer` `.review` | 妙想 | **妙想**：浅蓝紫渐变 + 白卡 + 蓝紫 |
| `#chat`、复盘里的揭晓 | 微信 | **微信**：`#ededed` 底、`#95ec69` 绿气泡 |

**每一层都必须像它自己**，切换的落差才是叙事：

> 你在东财 App 里按下转账 → **妙想**把你拦下来 → 你打开的是一段**真的微信记录**。

把三层刷成同一个色，这条线就没了。这是这一页最重要的一条，
下一次有人提"统一一下视觉"时，先读它。

### 为什么聊天屏不能是妙想的样子

这一屏的全部说服力在于**玩家相信对面是个人**——老陈是个正在被骗的股民，
他用微信。把气泡换成妙想的蓝紫，他立刻从"一个人"变成"一个 AI 助理"，
而角色对调的前提当场没了。

2026-09-04 之前有一版把三层收进同一套暖灰纸面：好看，但它**谁都不像**。
那一版的气泡是 `#dce8e1`（沙绿），既不是微信也不是妙想。

## 色

### 妙想（外壳五屏）

取自 App 实拍四屏（聊天 / 助手 / Claw / 开屏）。

| token | 值 | 用在哪 |
|---|---|---|
| `--mx-canvas-top/mid/bot` | `#e8ecfb` → `#f2f4fd` → `#fbfcfe` | 屏底渐变。**三段不是两段**：中间那档让过渡在手机高度上不出色带 |
| `--mx-card` | `#ffffff` | 卡。**无描边**——描边加阴影是幽灵卡，两者只留一个 |
| `--mx-ink` / `--mx-muted` | `#1a1a26` / `#69697b` | 正文 / 副文字。副文字**取最差的那个底算**：渐变顶 4.56:1、白卡 5.37:1，两处都过 AA |
| `--mx-brand` | `#2b5ce6` | 主按钮、强调、焦点圈 |
| `--mx-violet/blue/mint` | `#7b3fe4` `#1e8cf0` `#3bd98a` | 只给三叶标记本身 |
| `--mx-shadow` | 有偏移 + 柔化 | 不是零偏移的彩色光晕 |
| `--mx-radius` | `18px` | 卡。小控件用胶囊 |

### 微信（聊天与揭晓）

照实拍单聊与群聊逐个取。唯一一处偏离：`--wx-sub` 从真微信的 `#b2b2b2`
（白底 2.3:1）压到 `#757575`（白底 4.61:1）。

**算它的时候要看它印在哪**：`--wx-sub` 一处都不在聊天区里——它印的是复盘揭晓的
时间、逐轮那一列、工作台时间，全在白卡上。拿聊天底 `#ededed` 去算是错的，
拿白底算才对。13px 上肉眼分不出差别，但它过 AA。

### 东财（转账屏）

`--brand-orange: #ea5404`，取自官网首页实测最高频色值。
**只在这一屏**：玩家在东财 App 里按下的那一下。人被拦下来之后接手的是妙想，
按钮跟着换手，两个色在同一条流程里各出现一次，交接看得见。

## 做法：改 token，不改规则

`style.css` 有 2600 多行、200 多处写死的色值。换世界的做法是
**在那几屏的作用域里把既有 token 重新指向新的一套**：

```css
#opening, #home, #primer, .review { --paper: var(--mx-card); --navy: var(--mx-brand); … }
```

改 token 只有一处会错，改规则有两千处会漏。
写死的那些（`.msg.me .bubble { background: #dce8e1 }` 这类）单独在文件末尾盖掉，
每一条都写清楚盖的是谁。

## 吉祥物

白色机甲牛（牛角＝牛市）**只出现在复盘**，因为那一屏是妙想在讲评——
它有真实职责。**不进聊天屏**，进去老陈就变成机器人了。

当前用的是矢量三叶标记（`static/assets/miaoxiang-mark.svg`，仓库自带）。
要换成那只牛：把 PNG 放到 `static/assets/miaoxiang-mascot.png`，
改 `review.js` 里那一行的 `src`，并给 img 加 `mx-coach-face--photo`。
**只有这一处要改。**

两个坑，都在 2026-09-04 实测栽过，别再踩：

- **不要写"先请求 PNG，挂了再退回 SVG"**：素材不在时那是一次 404，
  而 E2E 有一条「全程一条控制台报错都没有」，404 会让它当场红。
- **不要用内联 `onerror`**：CSP 是 `script-src 'self'`，内联事件处理器被拦，
  既不生效还多报一条安全错。

## 边界

可以写"设计为接入妙想"，**不能写"妙想已上线这个功能"**（它没有）。
这条不是品牌限制，是事实准确性——见 [docs/CONTEST.md](docs/CONTEST.md) 第五节。

## 数字必须是算出来的

这一页所有对比度都由脚本算，**不许手写估值**。2026-09-04 复核时发现上一版
四个数全是错的（4.62 实际 4.92、4.61 实际 3.45、5.02 实际 5.27、5.31 实际 5.98），
其中两个还掩盖了真的不合格：`--mx-muted` 印在渐变上只有 4.17:1、
`--wx-sub` 印在白卡上只有 3.45:1。

**两个教训，都写在这儿**：

1. **拿最差的那个底算**，不是拿最好的那个。同一个色印在白卡和渐变上不一样，
   门槛要按渐变那一侧定。
2. **先查它到底印在哪再算**。`--wx-sub` 我一度按聊天底 `#ededed` 去算，
   而它一处都不在聊天区里——查清用处比选色重要。

## 换底就要重算，不只是换色

2026-09-04 把外壳换成妙想渐变、把聊天换成真微信灰之后，有三处**颜色一个字没改
却掉出 AA** —— 因为它们脚下的底变了：

| 处 | 旧底 | 新底 | 旧 | 新 |
|---|---|---|---|---|
| `#mood` 情绪档位 | 纸底 `#eceeea` | 微信灰 `#ededed` | 4.20:1 ✗ | 4.59:1 ✓ |
| `.live-label` 异动标 | 纸底 | 妙想渐变 `#e8ecfb` | 4.17:1 ✗ | 4.56:1 ✓ |
| 发送键白字 | 东财橙 | 微信绿 `#07c160` | **2.38:1 ✗** | 4.61:1 ✓ |

发送键那条最险：白字压微信绿只有 2.38:1，连大字号的 3:1 都够不到，
而它是全屏最常按的键。现在用 `#048743`——同一支绿深一档，
读起来像"按下去的那一档"，不像换了品牌。`--wx-brand` 本身不动，它还印着别处。

`--signal` 从 `#bb4e3f` 到 `#b14a3b` 同理：**每一次调它都是因为它脚下的底变了**，
不是因为有人觉得该更红一点。

（这三条是 2026-08-29 / 08-30 两份界面评审点名的六处未达标里剩下的部分，
连同"档位提示写死代词""七种问法面板键盘只够得到取消"一并已修完，
评审原件已删——结论留在这里。）
